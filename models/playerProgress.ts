export type playerProgress = {
  player_id: string;
  current_floor: number;
  current_room: number;
  rooms_cleared: number;
  floors_cleared: number;
  bosses_defeated: number;
  longest_resolve: number;
  focus_minutes: number;
};
